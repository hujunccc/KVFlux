add_executable(kvflux_benchmark block_manager_benchmark.cpp)
target_link_libraries(kvflux_benchmark PRIVATE KVFlux::kvflux)

if(KVFLUX_ENABLE_CUDA)
  # 只有模拟计算基准包含 kernel；核心库仍只依赖 CUDA Runtime。
  enable_language(CUDA)
  add_executable(kvflux_transfer_benchmark transfer_benchmark.cpp fake_compute.cu)
  target_link_libraries(kvflux_transfer_benchmark PRIVATE KVFlux::kvflux)
  set_target_properties(kvflux_transfer_benchmark PROPERTIES CUDA_STANDARD 17 CUDA_STANDARD_REQUIRED ON)
  add_executable(kvflux_prefetch_benchmark prefetch_benchmark.cpp fake_compute.cu)
  target_link_libraries(kvflux_prefetch_benchmark PRIVATE KVFlux::kvflux)
  set_target_properties(kvflux_prefetch_benchmark PROPERTIES CUDA_STANDARD 17 CUDA_STANDARD_REQUIRED ON)
  add_executable(kvflux_cache_benchmark cache_benchmark.cpp fake_compute.cu)
  target_link_libraries(kvflux_cache_benchmark PRIVATE KVFlux::kvflux)
  set_target_properties(kvflux_cache_benchmark PROPERTIES CUDA_STANDARD 17 CUDA_STANDARD_REQUIRED ON)
  add_executable(kvflux_v2_benchmark v2_paged_benchmark.cpp)
  target_link_libraries(kvflux_v2_benchmark PRIVATE KVFlux::kvflux)
  # CUPTI Activity 可记录真实 GPU kernel / memcpy 时间，不依赖 Nsight GUI。
  find_path(KVFLUX_CUPTI_INCLUDE_DIR cupti.h)
  find_library(KVFLUX_CUPTI_LIBRARY cupti)
  if(KVFLUX_CUPTI_INCLUDE_DIR AND KVFLUX_CUPTI_LIBRARY)
    add_executable(kvflux_v2_cupti_profile v2_paged_benchmark.cpp)
    target_compile_definitions(kvflux_v2_cupti_profile PRIVATE KVFLUX_V2_CUPTI_PROFILE=1)
    target_include_directories(kvflux_v2_cupti_profile PRIVATE ${KVFLUX_CUPTI_INCLUDE_DIR})
    target_link_libraries(kvflux_v2_cupti_profile PRIVATE KVFlux::kvflux ${KVFLUX_CUPTI_LIBRARY})
  endif()
endif()
